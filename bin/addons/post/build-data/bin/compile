#!/usr/bin/env bash

echo "Do data ops"

if [ -n $DATABASE_URL ]; then

    cwd_cache=$PWD
    cd $BUILD_DIR
    project/manage.py migrate --noinput --settings=$DJANGO_SETTINGS_MODULE
    cd $cwd_cache

fi
